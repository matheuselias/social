<p id="notice"><%= notice %></p>

<h1>Listing Enderecos</h1>

<table>
  <thead>
    <tr>
      <th>Tipo</th>
      <th>Endereco</th>
      <th>Cidade</th>
      <th>Bairro</th>
      <th>Estado</th>
      <th>Numero</th>
      <th>Cep</th>
      <th>Telefone</th>
      <th>Familia</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @enderecos.each do |endereco| %>
      <tr>
        <td><%= endereco.tipo %></td>
        <td><%= endereco.endereco %></td>
        <td><%= endereco.cidade %></td>
        <td><%= endereco.bairro %></td>
        <td><%= endereco.estado %></td>
        <td><%= endereco.numero %></td>
        <td><%= endereco.cep %></td>
        <td><%= endereco.telefone %></td>
        <td><%= endereco.familium_id %></td>
        <td><%= link_to 'Show', endereco %></td>
        <td><%= link_to 'Edit', edit_endereco_path(endereco) %></td>
        <td><%= link_to 'Destroy', endereco, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Endereco', new_endereco_path %>
